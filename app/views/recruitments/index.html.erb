
<div class="wrapper">
  <%= render 'upper' %>
  <section class="main">
    <div class="main-p">
      <%= render partial: 'recruitment', locals: { recruitments: @recruitments } %>
    </div>
  </section>
</div>
